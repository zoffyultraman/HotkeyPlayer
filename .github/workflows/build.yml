name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore HotkeyPlayer/HotkeyPlayer.csproj

      - name: Build
        run: dotnet build HotkeyPlayer/HotkeyPlayer.csproj -c Release --no-restore

      - name: Publish
        run: dotnet publish HotkeyPlayer/HotkeyPlayer.csproj -c Release --no-build -o ./publish

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: HotkeyPlayer
          path: ./publish/*.exe
